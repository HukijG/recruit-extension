node_modules
.plasmo
build

# Reference/debug files
export.csv
*.html
rf-candidate-added-information.json

# Environment files (.env.example is committed; the real ones are not)
.env
.env.*
!.env.example

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Root project uses pnpm; npm lockfiles at this level are a stray.
# mobile/ subapp is on npm so its lockfile is exempted explicitly.
package-lock.json
!mobile/package-lock.json
